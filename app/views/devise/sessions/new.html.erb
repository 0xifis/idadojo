<h2 class="subheader">Sign in</h2>
<hr />

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, :required => false, :autofocus => true, :placeholder => "Email" %>
    <%= f.input :password, :required => false, :placeholder => "Password" %>
  </div>
<div class="row">
	<div class="small-11 small-centered large-3 large-offset-3 columns form-offset">
		 <%= f.button :submit, "Sign in", class: "expand" %>
	</div>

</div>

<%= render "devise/shared/links" %>
<% end %>
